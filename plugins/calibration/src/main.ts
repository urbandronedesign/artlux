// @artlux/plugin-calibration/main — main-process barrel. Single identity for the native manager.
// NEVER pull renderer code.

export * as calibManager from './calibManager';
export { plugin } from './plugin.main';
